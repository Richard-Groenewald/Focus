# Focus CRM — Project Context

This file is read by Claude Code at the start of every session. It is the source of truth for project conventions, gotchas, and architecture. Keep it current.

## Product

Focus CRM (formerly SalesFlow) — internal CRM for **Xone Integrated Security (Pty) Ltd**, a South African integrated security solutions company. Used by sales and management to track opportunities, contracts, leads, and engagements across mining, hospitality, residential estates, and FMCG sectors.

- **Current version:** v7.9.76 (Support Items page follow-ups, Richard 2026-09-20, three asks: "moving from one tab to the next creates a short lasting artifact — rather blank the screen before it is written with the right data; if there are no unfiled items in a tab, don't display unfiled; make these types of descriptors editable and put them in a light contrasting colour on a line of their own". (1) `renderPbItemCatalogPage` no longer leaves the previous kind's rows under an empty head while the fetch runs: unless the body's first row carries `data-pbi-kind` equal to the kind being opened (a same-kind reload after a save or drop keeps its rows), it draws `pbiHeadHtml(kind)` — strip, toolbar, description, column header — and a one-row "Loading…" body at once; `pbiPaint` stamps `data-pbi-kind` on every group row and on the "No items yet." row. (2) An Unfiled group with no items is not rendered (an item can still be unfiled from its editor's Subcategory select; "+ Add <item>" re-shows Unfiled with the new item). (3) The description is its own pale row (`#EEF3F8`, normal weight) under the toolbar with an Edit button: `pbiDescriptor(kind)` returns the setting `pb_catalog_hint_<kind>` when non-blank, else the standard wording built from `PB_ITEM_KINDS` (`hint` + how the item is offered + the never-edit-a-cost sentence); `pbiHintEdit` / `pbiHintSave` / `pbiHintCancel` edit it in a textarea (`pbItemAdmin.hintEditing` / `hintDraft` survive repaints), saved by PATCH-then-POST on `settings.key` like the other settings and mirrored into `appSettings`; blank = back to the standard wording. Keys are new rows in `settings`, no migration. As of 20 September 2026. v7.9.75 was Catalogue pages open collapsed after login, Richard 2026-09-20: "after login, open all items under proposal builder collapsed, thereafter remember state until next login" — the first paint of each kind (Support Items' three tabs, Uniform Types, Training Courses) after a login adds every group and subcategory key to `pbItemAdmin.collapsed` once (`collapsedInit`), and from then on the set is whatever the user left, kept per kind by `_pbItemAdminByKind`; `enterApp` calls `pbiResetSession()` (clears the stash, a fresh `pbItemAdmin`, `_pbSupportLastKind` back to Workforce) so the next login starts collapsed again. Expand all / Collapse all and "+ Item" (which expands its group and subcategory) are unchanged. No SQL. Same day, data only, both databases: the `a1` test row (quote_accessories 26, one cost row, nothing else referencing it) deleted — `sql/delete_a1_test_item.sql` is the record; and the four seeded placeholder uniform types (Standard / Formal / Tactical / Reception) deleted — `sql/delete_seeded_uniform_types.sql`, one sandbox post lost its pick, two trial costs cascaded. As of 20 September 2026. v7.9.74 was Proposal Builder catalogue pages: one-line items, deeper indents, collapsing groups, one Support Items page, and every kind gets the tree, Richard 2026-09-19, three asks: "make every item 1 line, just give the name, in use from, cost, state … when one clicks, it allows edits and adding costs; make categories, sub categories and items much more indented … everything under a category indent should be at the same indent at least; allow categories and subcategories to collapse" / "in settings menu, put all support items categories under one heading Support Items, and combine Standard Posts and Standard Headings into one heading" / "make all support items and uniforms and training in proposal builder work exactly like workforce support items". `renderPbItemCatalogPage(kind, tabs)` fetches once and `pbiPaint()` repaints from `allData`, so open / close / collapse cost no traffic; an item row is name · in use from · current cost · state with a ⠿ handle and a ▸ caret, and `pbItemToggle(id)` on the row opens the panel beneath it (Name / Subcategory / In use from / Retired inputs with Save · Retire · Close, then the cost history and "+ New cost from date"). `pbItemAdmin` = `{ kind, open, addingCost, dirty, draft, costDirty, costDraft, collapsed: Set, tree, drag, tabs, costsMissing }` (`editing` is gone), kept per kind in `_pbItemAdminByKind` while another catalogue page shows, so a visit to Training Courses and back finds Workforce's open item, drafts and collapsed groups as they were: every keystroke in the panel calls `pbiTouch()` (dirty + a snapshot of the four `pbi-*` inputs into `draft`) and in the cost form `pbiTouchCost()` (`costDirty` + `costDraft` of the `pbc-*` inputs); `pbiPaint` writes both back after every repaint with `pbiRestore` (a select only to an option it still has, so a subcategory deactivated meanwhile never turns a drafted pick into "unfiled") and deliberately never reads the DOM itself — the body may still hold another kind's page whose inputs would overwrite this kind's draft (a real bug caught in mock mode). `pbiBusy()` refuses a row switch, a tab switch, Retire, a new item, and a subcategory deactivate or remove while either draft is dirty (an untouched cost form blocks nothing); `pbiDropMode` returns null while either is dirty; Cancel, Save cost, Close and a row toggle clear the cost draft (`pbiClearCostDraft`). Save PATCHes `subcategory_id` only when it changed (a table without the column still saves the rest) and keeps the panel open, and a failed save or cost save does not re-render. Indents are fixed: group 14px, subcategory 42px, item 70px whether or not a subcategory sits between (`PBI_IND` = 28), panel 90px. A group or subcategory row click collapses it (`pbiCollapse('g', index)` / `('s', id)`, keys `g:<groupName>` / `s:<id>` in `pbItemAdmin.collapsed`, collapsed rows read "· N items", Expand all / Collapse all in the toolbar); "+ Item" on a subcategory or a single-leaf group files the new item there (`pbItemAdd(subId)`) and expands its rows. `PB_ITEM_KINDS[kind].sub` names each kind's subcategory family (workforce / contract / service / uniform / training): `pbSubcatTree`, `pbiPersistOrder`, `pbiAddGroup` / `pbiAddSub` and `pbChecklistGrouped` read it, so Contract, Service, Uniform Types and Training Courses get the same tree editor, the per-post Training checklist and the Contract tab's two lists group by it, and the post row's Uniform dropdown gets `<optgroup>`s (`pbUniformOptions`). Sidebar: `pb_support_items` (`renderPbSupportItemsPage`, Workforce / Contract / Service as a tab strip via `pbAdminTabStrip` + `pbiTab`, remembers the last tab) replaces `pb_accessories` / `pb_contract_items` / `pb_service_items` / `pb_support_subcategories` — the generic subcategory page and its `PB_SCHEMAS` entry are gone, each tree editor manages its own family; `pb_standard_names` (`renderPbStandardNamesPage`; `pbCatalogTabs` is drawn by `renderPbEditor` itself so the strip survives its re-renders; `pbCatalogTab`) replaces `pb_standard_posts` / `pb_standard_headings`. SQL `sql/support_subcategories_all_kinds.sql` — additive, re-runnable, run on BOTH databases, may precede the code: widens the category check to uniform / training and adds `subcategory_id` (RESTRICT FK) to `quote_uniform_types` and `quote_training_courses`; no seed rows, Richard builds each hierarchy on its page. Mock seed gains training and uniform families and `quote_uniform_types` rows. Two adversarial review rounds (the first cut short by the usage limit at 2 confirmed, the second 13 agents: 9 candidates, 6 confirmed, 3 refuted) fixed, pre-commit: the cost form's input was outside the unsaved-input guard (now `costDirty` / `costDraft`); a subcategory Deactivate / Remove while a drafted pick pointed at it turned the pick into "unfiled" on Save (both guarded, and `pbiRestore` never sets a select to a vanished option); `pbiDrop` spliced `pbItemAdmin.tree` in place, so a group-bar click, "+ Subcategory" or Rename in the seconds before the reload acted on the wrong group (it works on a copy now); a keystroke during the Save round trip was wiped by the post-PATCH reset (dirty/draft now clear before the await, re-marked on failure); Standard Posts & Headings left `pbActiveSchema` set so the next Human Resources visit drilled into that editor (the HR page now honours only its own tiles); the quote's placeholder texts still named the retired sidebar pages (`PB_ITEM_KINDS[kind].where`); a legacy workforce item priced per contract read "Unfiled" on the Contract tab (it now lists under "Workforce support items priced per contract" with its own family's headings); the Support Items page forgot its tab after a visit to Uniform Types or Training Courses (`_pbSupportLastKind`, and a tab holding unsaved input wins). Verified in mock mode: open / dirty / blocked row switch / collapse-and-restore draft / save / close, collapse all, contract group + subcategory + item creation, drop into a subcategory (also collapsed) and refused while a cost form is open, training and uniform pages, the standard-names strip surviving a re-render, the Contract tab grouped by the new contract family. v7.9.73 was the Workforce Support Items page becoming a drag-and-drop tree editor, Richard 2026-09-19: "allow creation of hierarchy; allow the item order to be adjusted by dragging, remove visible order; also allow hierarchical items to be ordered and dragged" — on every item catalogue page (`renderPbItemCatalogPage`) the Order column and the order input are gone; each item row carries a ⠿ handle and is a drop target (`pbiDragStart` / `pbiDragOver` / `pbiDrop` / `pbiDragEnd`, row classes `pbi-dragging` / `pbi-drop-above` / `pbi-drop-below` / `pbi-drop-into`). On a kind with a category (workforce today) the page renders `pbSubcatTree(items, category, true)` — empty subcategories and an always-present Unfiled group stay visible as drop targets — into `pbItemAdmin.tree`: group bars (navy; ⠿, Rename, + Subcategory) and subcategory bars (⠿, Rename, Deactivate/Reactivate, Remove when empty), plus "+ Add group" beside "+ Add <item>" (`pbiAddGroup` creates a group of one — a leaf whose name is the group's; `pbiAddSub` adds a leaf under it; `pbiRenameGroup` renames every leaf's group_name and, for a group of one, its name too; `pbiRemoveSub` is refused by the RESTRICT FK while items are filed). Drops: an item beside another item or onto a subcategory / a group-of-one / Unfiled (re-files it); a subcategory beside another or onto a group bar (re-parents it, the emptied group disappears); a group beside another (Unfiled stays last). `pbiDropMode` says which drops are legal. After a drop `pbiPersistOrder` renumbers subcategories 1..N and items 1..M in tree order and PATCHes only the rows whose `display_order`, `group_name` or `subcategory_id` changed (parallel PATCHes — a partial upsert would trip NOT NULL on the insert half). `pbGroupBySubcat(items, category, includeEmpty)` and `pbSubcatTree(items, category, includeEmpty)` gained the flag; `single` is now decided per group (one leaf named as the group), so a leaf dropped beside a group-of-one's leaf turns it into a real group. The separate Support Item Subcategories page still exists (it is where a subcategory's category would be changed); no schema change. A review pass (cut short by the usage limit — one reviewer, five unverified candidates, each then confirmed by hand) fixed: a subcategory dropped onto a group bar resolved the target by index AFTER its emptied source group was spliced out, so it could land one group over or hit Unfiled with a NULL group_name (target now resolved before any removal); items could be dragged into a deactivated subcategory (refused, like the edit select); a drop while an item's editor was open re-rendered away typed input (drops refused while `pbItemAdmin.editing` is set); the last legal row kept its red bar while the cursor sat on an illegal target (the sweep now runs before the legality check); the Unfiled "no items — drag one here" row had no drop handlers. As of 19 September 2026. v7.9.72 was subcategory hierarchy as a tree, Richard 2026-09-19: "OFFENSIVE SUPPORT / Lethal / 9mm Parabellum" — the three grouped displays (Workforce Support Items admin page, a post's Workforce Support Items checklist, the quote's Workforce Support Items tab) now render GROUP → subcategory → items instead of one flat "Offensive · Lethal" heading per leaf: `pbSubcatTree(items, category)` builds the tree from `pbGroupBySubcat` (groups in first-appearance order; a leaf that is its own group — Communications, Evidence and Recording, Welfare, Unfiled — is `single` and collapses to one level), `pbSubName(s)` is the leaf's name (+ "(inactive)"), group headings are uppercase navy bars, subcategory headings indented, item rows indented one level further (the admin page and Workforce tab pass an `indent` px to their `rowOf`). Seed group names become "Offensive Support" / "Defensive Support" (the SQL renames an already-seeded database keyed on code, only while it still carries the old name; the mock seed matches). `pbSubcatLabel` ("Offensive Support · Lethal") remains for the admin page's Subcategory cell and edit select. No schema change; as of 19 September 2026. v7.9.71 was Support item subcategories, Richard 2026-09-19: "I want to create a number of subcategories to make working with them easier" — agreed list Offensive (Lethal · Less-lethal · Impact · Restraint), Defensive (Protective · Detection · Lighting), Communications, Evidence and Recording, Welfare. New table `quote_support_subcategories` (category workforce | contract | service, `group_name`, `name`, `display_order`, `active`; ten workforce rows seeded; a leaf that is its own group repeats the name) and `quote_accessories.subcategory_id` (FK, ON DELETE SET NULL); the migration files today's workforce items by code (the `a1` test row stays unfiled). Admin: a new sidebar page Proposal Builder → Support Item Subcategories on the generic catalogue editor (`PB_SCHEMAS['support-subcategories']`, columns Group / Subcategory / Display Order / Active; `readOnly: true` in PAGES like the other pb_* pages); the Workforce Support Items page gains a Subcategory column (select while editing, `pbItemSave` sends `subcategory_id` only when that select rendered) and groups its rows under subcategory header rows, unfiled last. Helpers `pbSubcats(category)`, `pbSubcatOf(item)`, `pbSubcatLabel(s)` ("Offensive · Lethal"), `pbGroupBySubcat(items, category)`, `pbChecklistGrouped(kind, items, rowOf)`. A post's Workforce Support Items checklist and the quote's Workforce Support Items tab group by subcategory the same way; contract / service items and the Conditions & Rules pickers are unchanged (the table carries a category so they can join later). `openQuote` fetches `quote_support_subcategories` in the `catP` batch (absent table → none, everything renders flat). SQL in `sql/add_support_subcategories.sql` — additive, re-runnable (it also upgrades a database that ran its first cut), run on both databases before or with the code. An 8-agent adversarial review found five distinct real issues, all fixed pre-commit: seeded rows now carry a stable `code` (`wf_lethal` …) and the seed keys on it (a rename on the admin page no longer resurrects the old name on a re-run); the composite UNIQUE on (category, group_name, name) is gone (the generic editor inserts a blank placeholder row on "+ Add row", so a name key refused a second add and a retire-and-re-add); the FK is plain REFERENCES (RESTRICT) so deleting a subcategory that still files items is refused by the database — set it inactive instead; `pbSubcats(category, true)` returns inactive ones too and `pbGroupBySubcat` groups by all of them, so an item filed under a since-deactivated subcategory keeps its heading (labelled "(inactive)") and the edit select keeps the item's own pick, so an unrelated edit never nulls `subcategory_id`; the unfiled group is "Unfiled" everywhere (the checklist said "Other"); the one-time backfill runs only while nothing has been filed yet. As of 19 September 2026. v7.9.70 was Workforce Support Items tab reworked, Richard 2026-09-19, seven numbered questions and instructions on a screenshot: the static legend row (as per rule / above rule / below rule / no cost-retired) is gone; the footer totals and the closing paragraph are gone and ONE monthly total sits above the table (with a "plus R x once-off at setup" line only when any item recovers once-off), beside a note "Priced as at <contract start>, the contract start date" in place of the repeated tab title; every item is now a collapsible row — name, units (the quoted quantity), monthly, status — that opens (`pbWfToggle`, `_wfState.open`) to the full detail: posts ×qty, from-posts quantity, adjustment + reason, quoted, unit/month, basis, recovery, once-off, warnings, and the Adjust button or the inline adjustment editor (`pbWfAdjustOpen` now also opens the row). Wording: "rule" is retired on this tab because it is Conditions & Rules' word — the baseline is what the ticked posts need by the item's basis, so statuses read `from posts` (ok) / `added here` (adjusted up) / `reduced here` (adjusted down, warn) / `here only` (no post ticks it, quantity entirely from this tab, info) / `not ticked` (none, grey — an unticked item shown via "show every item", or an adjustment that quotes nothing) / `no price` / `retired` / `not in use` (bad), and the editor, confirm dialog, clear dialog and live preview say "what the posts need" / "From posts" rather than "the rule total". `pbWorkforceRollup`'s `level`/`label`/`warnings` carry the new words; its shape, `pbWfFinal`, `pbWfMoney`, `pbAccSummary`'s "Workforce adjustment" line and `pgPricing` are untouched. A 15-agent adversarial review found seven distinct real issues, all fixed pre-commit: a header-row click while that row's editor was open re-rendered and discarded typed input (now a no-op while editing); a zero-quantity adjustment on an unticked item read "here only"; "show every item" rows read "from posts" with 0 units and their detail said "added on this tab"; `_wfState` carried expanded/editing rows into the next quote (reset in `openQuote`); the header badge said "unpriced or retired" for rows that were "not in use"; the detail's Unit / month did not reconcile with Monthly for once-off items (now shows the running cost per unit plus a "Unit once-off"); the old header comment and this file's v7.9.53 bullet still spoke of "rule". No SQL; as of 19 September 2026. v7.9.69 was Posts table Grade and Uniform columns, Richard 2026-09-19: "size the grade column so the gap on either side is the same as on the current left side… centralise… for uniform, fix the column to half its current width, left justify header and contents" — the column-divider rule pads every cell 3–6 by 10px on the right while the divider itself sits 5px into the 10px gutter, so Grade's select had 5px on its left and 15px on its right; child 4 now has `padding-right:0` in both the header and the row and its track drops from 44px to 34px, leaving 5px on each side with the select the same size as before (header and contents already centred). Uniform's track goes from `minmax(170px, 1fr)` to a fixed 300px (about half of what it resolved to on a ~1400px editor), `.pb-summary-uniform-sel` and its cell read left instead of centre, the header already did; the leftover width moves to the actions track, `minmax(255px, 1fr)`, so Delete/Duplicate stay flush right under the heading bar's Delete. Grid templates changed in all three places (`.pb-post-summary`, its heading-bar variant, `.pb-posts-column-header`). No SQL; as of 19 September 2026. v7.9.68 was the quote editor Posts table losing its #/Shift and Officers Required columns, Richard 2026-09-19 with a screenshot: "remove these two columns" — the two `.pb-post-officers` spans and their header cells are gone from the summary row, the three matching grid templates (`.pb-post-summary`, the heading-bar variant, `.pb-posts-column-header`) drop the 80px + 100px tracks, the column-divider rule now spans children 3–6 instead of 3–8, and the `.pb-post-officers` style is deleted; `pbNoPerShift` / `pbOfficersForPost` stay (the expanded post's Hours summary, the totals band and the printed proposal's scope table still show both figures). No SQL; as of 19 September 2026. v7.9.67 was the stray header "+ Add" is gone from every Proposal Builder admin page, Richard 2026-09-19: "remove the add button on proposal builder rules" — `showPage` wires the generic `#add-btn` to `openAddModal()` for any page without a special case, and every `pb_*` page is `custom: true, fields: []`, so on all fifteen it opened an empty form while each screen already carries its own add control ("+ New condition", "+ Add row", "+ Add <item>", the HR tiles). Fixed the way Dashboard / Milestones / Reports already do it: `readOnly: true` on the fifteen `pb_*` PAGES entries, which only hides that header button for a custom page (the generic table renderer's "Read only" row actions never run for custom pages). No SQL; as of 19 September 2026. v7.9.66 was Conditions & Rules, Richard 2026-09-18 from the two-tile screenshot: "combine these two in a single conditions/selection table that also allows allowances to be auto selected based upon other criteria while keeping the functionality in incentives and allowances" — ONE table pair, `quote_condition_rules` / `quote_condition_rule_members` (rule_type exclusive | requires | implies; member role member | trigger | target; kind statutory | discretionary | incentive | post | support | training), replaces BOTH `quote_allowance_rules`/`_members` and `quote_support_item_rules`/`_members`, and the two admin tiles become one sidebar page, Proposal Builder → Conditions & Rules (`renderCondRules` / `_condPaint` / `_condSave` / `_condEdit` / `_condToggle` / `_condDelete`; `_ccActive`, `PB_CC_TILES`, `pbOpenCc`, `pbBackToCcGrid`, `renderAllowRules`/`_ar*`, `renderSupportRules`/`_sup*` retired). The evaluator is ONE fixpoint: `pbSupportEval()` starts from `pbManualActiveSet(p)` (manual ticks + the hardcoded night rule + manual support/training ticks) and layers every active implies rule's targets — statutory allowances (new `.statutory[postId][code]` bucket), support items (per post, or once on the quote by cost basis) and training — until nothing new ticks, then `out.active[postId]` is the settled Set (rebuilt once more after the loop); `pbActiveAllowSet(p)` is now a thin reader of it (`pbImpliedStat` retired). That is what lets a support item or training course drive an allowance, impossible while allowances resolved before support items ever saw the board. implies targets stay statutory-only on the allowance side (discretionary/incentive carry an amount with no default) and a statutory target with no row effective at the quote's date is inert; post conditions stay trigger-only. `pbToggleChecklist` now runs `pbRuleConflict` / `pbRuleRemovalBlock` (checklist kind accessory → rule kind support) so a support item or course can genuinely be an exclusive/requires member; contract-level items (`pbToggleContractAcc`) are deliberately NOT conflict-checked — that enforcement is post-scoped by design. The memo `_pbSupportEval` is cleared at every tick/amount mutation and at the top of both conflict checks, so a check never judges a stale set. Editing a rule and switching its type asks before dropping members the new type cannot hold (role-aware). SQL in `sql/unify_condition_rules.sql` — NOT additive: it migrates the rows (allowance rules keep ids; support rules become implies rows with new ids via a temp id map; the sequence is bumped BEFORE the auto-id inserts), drops the four old tables, RLS on both, statement-level audit on the rules table and row-level `audit_row_change()` on the members table (no id column, and `audit_stmt()` joins on id). Run it in the SAME window as the code deploy. Proven in mock mode against the exact prod rule shapes (Armed role, Seniority, the armed-post implies rule), a training→statutory implies, a 13-item implies chain, exclusive/requires blocks on checklist ticks and on amount entry with no render in between; an 11-agent adversarial review found 7 candidates — 5 real and fixed (sequence order, audit trigger, stale `out.active` at the cap, unresolvable statutory targets, stale memo), 2 cosmetic and fixed; as of 19 September 2026. v7.9.65 was Uniform Types gaining real costs, Richard 2026-09-18: "uniforms have costs and these should be managed under proposal builder similar to workforce support items" — `quote_uniform_types` keeps its own table (a post still picks exactly one via the dropdown, never the multi-tick checklist Workforce Support Items use) but is upgraded to the exact shape `quote_accessories` uses (`code`, `in_use_date`, `retired_date` added) and gains its own dated cost table, `quote_uniform_type_costs`, mirroring `quote_accessory_costs` field-for-field. `PB_ITEM_KINDS` gains one more entry, `uniform` (`model: 'accessory'`), and that is the ENTIRE new admin surface — `renderPbItemCatalogPage`/`pbItemAdd`/`pbItemSave`/`pbItemRetire`/`pbItemSaveCost`/`pbItemCurrentCost`/`pbAccMonthlyUnit` are all already generic against `PB_ITEM_KINDS[kind]`, so the cost-drawer admin page (dated rows, never edited; acquisition/life/running/loss/basis/recovery) came free. The Uniform Types admin page (Proposal Builder sidebar entry, unchanged from v7.9.64) now routes to `renderPbItemCatalogPage('uniform')` instead of the simpler v7.9.64 catalogue editor, which this supersedes. The post row's Uniform select now lists only uniform types currently in their in-use window, plus the post's own current pick even if it has since been retired (`pbItemRetire`'s own promise: "posts that already have it keep it"). `pbAccSummary()` gains one more loop, keyed by `p.uniform_id`, using the identical `add()`/`pbAccQtyForPost` math a ticked Workforce Support Item uses, folding into the SAME `byPost` accumulator — so a post's priced uniform reaches the proposal's per-post line (`pgPricing`) with no change to `pgPricing` itself. `openQuote`'s load gains two more parallel fetches (`quote_uniform_types` unfiltered, `quote_uniform_type_costs`) inside the existing `catP` batch. SQL in `sql/add_uniform_types.sql` (rewritten — nothing from the v7.9.64 draft was ever deployed, so this is a clean replacement, not a follow-on migration); still seeds four names with NO cost rows (nobody but Richard knows the real prices — "no cost yet" is the honest starting state). ⚠️ A same-night lesson repeated: removing a destructured variable from an array-destructuring line without removing its now-orphaned array ENTRY shifts every position after it — caught immediately by testing the actual open-a-quote flow, not just a syntax check. No SQL run anywhere; code not deployed; as of 18 September 2026. v7.9.64 was Uniform Types, Richard 2026-09-18, then the night's third ask: "uniform is individual, and common per post... remove the schedule heading and use that column for a uniform dropdown" — the post row's Schedule column (a compact shift-time preview; the full Shift schedule section inside an expanded post is untouched) becomes a Uniform `<select>`, styled and positioned exactly like Grade's (`.pb-summary-uniform-sel` shares Grade's caret-free, centred CSS). The dropdown draws on a new admin-managed catalogue, `quote_uniform_types` — a real table, not a hardcoded list, mirroring Standard Post Names exactly (id/name/display_order/active, statement-level audit, RLS) and reusing the SAME generic catalogue editor (`PB_SCHEMAS['uniform-types']` + `renderPbCatalogPage`) with zero new admin-rendering code — a new sidebar entry, Proposal Builder → Uniform Types, is the only new page. `quote_posts.uniform_id` (a real FK, like `grade_id`) replaces the old `quote_posts.uniform` free-text column, which was never wired to any input anywhere (only ever read back in the printed proposal and copied by Duplicate) and held zero rows on either database — nothing to migrate off it. `pgRenderScope` (the printed proposal's post table) resolves `uniform_id` to a name the same way it already resolves `grade_id`. Seeded with four starting names (Standard, Formal / No.1 Dress, Tactical / Response, Reception / Front of House) that Richard can rename, reorder, retire or add to himself from the new admin page — not meant to be final. SQL in `sql/add_uniform_types.sql`; run on both databases BEFORE the code (`openQuote`'s initial load and the admin page both read `quote_uniform_types` on first touch). Verified in the sandbox: header reads Uniform not Schedule, the dropdown lists and saves a selection, Duplicate carries `uniform_id`, the print table resolves the name (never leaks the raw id), and the new admin page lists/adds rows through the existing generic mechanism unmodified. v7.9.63 was quote editor Posts, four follow-ups from Richard 2026-09-18: (1) headings now drag to reorder by a ⠿ handle on the bar, exactly like posts already did — `pbHeadingBarDragOver`/`pbHeadingBarDrop` dispatch to the new heading-reorder handlers while a heading is being dragged and fall through to the existing post-into-heading handlers otherwise, so the bar keeps doing both jobs; Ungrouped is never draggable but IS a valid drop target (it always sits last, so dropping onto it means "move to the end", handled as a special case in `pbMoveHeadingTo` rather than a no-op); every ↑/↓ arrow button is gone, on headings AND posts (posts already had drag) — `pbMoveHeading`/`pbMovePost` and the now-unused `.pb-btn.arrow` CSS removed. (2) A post's Delete now comes before Duplicate (was the reverse), lining up under the heading row's own Delete button, which is its first action too. (3) The Grade `<select>`'s native caret is gone (`appearance:none`) and the column is narrower (70px → 44px; codes are single letters). (4) "Expand all posts" (which opened every post's detail panel) is retired; the button now toggles every heading's OWN collapse state via `pbToggleAllHeadings` — a heading being collapsed is what actually hides its posts, so this is what "show collapsed posts" needed; per-post expand-on-click is untouched. No SQL; as of 18 September 2026. v7.9.62 was a cosmetic follow-up from the v7.9.61 review: the empty-Ungrouped drop strip's `.pb-posts-area` inherited the global grey background, so it drew as a grey block with a dashed border; `.pb-ungrouped-strip .pb-posts-area { background:transparent }` makes it the dashed outline it was meant to be; no SQL; as of 18 September 2026. v7.9.61 was quote editor post names and moving, Richard 2026-09-18: the "Move this post to" select at the foot of an expanded post is removed with its handler `pbMovePostToHeading` and CSS — drag the ⠿ handle onto another heading instead (the existing drag-and-drop); the post-name input carries ONE control: the hidden `<datalist>`s that gave every name input a second native arrow are retired (`pbRenderDatalists` returns '' — the wrapper stays for `_pbSupportEval`), and the standard-name picker button is rendered only while the post is unnamed (`pbPostUnnamed`: blank or the 'New Post' / 'New Post (copy)' stub), always visible then via `.pb-name-dropdown-btn.always`, gone once a name exists and back if the name is cleared (a blank name is allowed — `quote_posts.name` is NOT NULL with no CHECK, '' is fine); the picker popup itself now WORKS — it was `position:absolute` at z-index 1100 in `<body>`, painted behind the fixed z-index 2000 editor, so it opened invisibly — now `position:fixed` at viewport coordinates, z-index 2500 like the copy prompt, closed by a scroll outside it (its own list scrolls freely — the listener filters on the event target and is removed on close), flipped above the input when it would run off the bottom and clamped sideways to the viewport; an EMPTY Ungrouped now renders as a slim dashed drop strip (`.pb-ungrouped-strip`) whenever named headings exist, so a post can still be dragged out of its heading — the one thing only the removed select could do; a blank-named post's duplicate stays blank and a bare '(copy)…' name counts as unnamed, so the picker returns; heading names keep their hover-reveal picker (fixed the same way) since a heading is always named at creation; no SQL; as of 18 September 2026. v7.9.60 was quote editor Posts collapse discipline, Richard 2026-09-18: every post starts collapsed — on opening a quote (already so) and now also when added or duplicated — and a post that is expanded, singly or via Expand all posts, shows all its sub headings collapsed (Shift schedule; Hours summary, listed only once Shift schedule is open; Allowances and Performance Incentives; Workforce Support Items and Training). Mechanics: `PB_DETAIL_DEFAULT_OPEN = {}`, the new top-level `pbExpandPost(postId)` is the one way a post becomes expanded and forgets that post's `pbDetailToggled` keys first, `pbToggleAllPosts` clears the set on its expand branch. Reverses v7.8.02's shift/hours default-open; heading GROUPS (`h.collapsed`) and the Weekdays column toggle (`p.weekdaysExpanded`, whose collapse rewrites Tue–Fri) are deliberately untouched; no SQL; as of 18 September 2026. v7.9.59 was the XSS escaping sweep: a shared module-scope `escHtml` / `escAttr` pair (defined once near the mock helpers) now escapes database and user values at every reachable HTML sink found with a canary harness in mock mode — the generic admin list/table renderer's fallback cell, all 55 `<option>` builders app-wide (dropdowns everywhere), the User Regions matrix name cell + checkbox titles + headers, the audit-log actor picker, the settings collaborator-role and people lists, the contact-form first/last name inputs, and note/description/textarea cells. Proof: seed poisoned with `<img onerror>` + quote-breakout names/notes, then every one of the 78 pages + the lead form + the contact modal driven → zero script execution, no double-escaping, normal data still renders plainly. The ~28 legacy per-function `esc`/`cEsc` helpers already escaped their own sinks and were left; new sinks should use `escHtml`. Deeper bespoke modals (quote editor posts, some engagement/milestone detail views) were not individually poison-tested — the harness in the scratchpad re-runs the sweep. v7.9.58 (the audit log stops holding credentials: `audit_scrub()` drops password_hash / password_salt / password from what BOTH audit trigger functions record — `audit_stmt()` and the row-level `audit_row_change()` — the historical rows are purged on both databases (29 prod / 21 dev), both functions gain a pinned search_path, and `sb.js` strips the same keys from `audit_log` responses as a backstop, keeping that endpoint plain like `system_users`; SQL in `sql/audit_log_scrub_credentials.sql`, tests in `tests/sb_test.js`; as of 18 September 2026. v7.9.57 was Support Items and Training: training courses join the Support Items conditions as triggers and targets — a course ticks per post when its trigger holds, a ticked course can trigger items or other courses, and a course a condition ticked may be overridden off with a reason; the Conditional Criteria tile is renamed Support Items and Training; SQL in `sql/training_conditions.sql`. v7.9.56 was session revocation (landed on `test` from a parallel session on 2026-09-17, commit c0b1b08): `system_users.session_version` rides in a v2 token and a BEFORE UPDATE trigger (`sql/add_session_version.sql`, applied to both databases before the code) bumps it on deactivation, any password-hash change or a forced reset; `sb.js` confirms active + version on every proxied request (60 s per-instance cache, cleared by any `system_users` write through the proxy), 503 on a lookup failure so a blip is not a sign-out, 401 `reason:'revoked'` otherwise, and the client names the cause on the sign-in screen; tests in `tests/sb_test.js`; every open session signs in once more. v7.9.55 was Conditional Criteria: a new Proposal Builder admin page with two areas — Incentives and Allowances (the allowance conditions, moved from Human Resources) and Support Items, where a named condition ticks support items of any category when an allowance or incentive is active on a post, a post condition holds, or another support item is ticked; workforce targets tick per post, contract and service targets once on the quote; an item a condition ticked may be overridden off with a reason; SQL in `sql/support_item_conditions.sql`. v7.9.54 was allowance conditions gain the always-on post condition `staffed` ("the post is staffed (always)", first in `PB_POST_CONDITIONS`, `pbPostCondition` returns true for every post) so a rule can apply to every post in a quote; no SQL. v7.9.53 was Workforce Support Items tab on the quote: each workforce item's rule total is summed over the posts that tick it by its basis, and the quote may add to, subtract from or override that total with a recorded reason after a warning; colour-coded status; the difference prices as its own line; SQL in `sql/workforce_support_adjustments.sql`. v7.9.52 was allowance conditions grow `requires` and `implies` rule types with trigger/target members and post-condition triggers (midnight shift, public holiday, Saturday, Sunday, weekend); SQL in `sql/allowance_rules_complex.sql`. v7.9.51 was support item categories: `quote_accessories.category` splits the old Accessories page into Workforce Support Items (things an officer carries or needs, per post), Contract Support Items (physical things a contract needs to be delivered) and Service Support Items (service things a contract needs to be delivered), the last two once per quote on the Contract Support Items tab; one table, one cost history; SQL in `sql/support_item_categories.sql`. v7.9.50 was start-date discipline: the revenue grid shows the current FY plus every FY holding a projection and the start date's FY, months before the start date are greyed, start date must be on or after the order date; an open opportunity whose start date arrives unsecured is suspended (new stage Suspended, probability 0, prior stage and probability kept on `deals.suspended_*`) by a once-per-session owner sweep on the dashboard and on open, with a forced review modal that restores stage and probability on a confirmed workable start date and shifts the stream; SQL in `sql/opportunity_suspension.sql`. v7.9.49 was Revenue Stream Generator: changing the service type warns and, when a stream exists, is allowed only together with clearing it (server-side delete of the months, margin and escalation reset to the new type's defaults); no SQL. v7.9.48 was edit opportunity: the revenue grid shows the current and next two financial years only, a past year only when it holds revenue (greyed tab), and past months are greyed and read-only; `getCurrentFY()` now follows `settings.fy_start_month` like `getFYMonths`; no SQL. v7.9.47 was Revenue Stream Generator is a compact pop-out modal opened from a button on the Proposal Builder tab, with the deal's service type shown and changeable there, margin as a field checked against the rule, and the grid's Margin row always naming the %; no SQL. v7.9.46 was annual escalation scoped to the service type: `quote_annual_escalations` rows carry `service_major_id` / `service_sub_id`, the generator picks sub → major → general for the start year, and the Manpower major is edited as a horizontal band above the Statutory Salary Rates grid; SQL in `sql/escalation_by_service_type.sql`. v7.9.45 was Revenue Stream Generator: the Proposal Builder tab's top block, collapsed by default, builds the opportunity stream before a proposal exists — the General tab's service type picks annuity or project via `service_sub.revenue_type`, escalation defaults from `quote_annual_escalations` by start year, the margin is asked for and checked against `quote_opportunity_margin_rules`, the probability is capped at `settings.rsg_probability_cap` (20), Clear deletes the months; Expected Order Date mirrored on the General tab; escalation now carries forward after the escalation month; SQL in `sql/revenue_stream_generator.sql`. v7.9.44 was accessory cost model: a dated cost row carries acquisition, life, running cost and loss allowance → one monthly unit cost, a basis that fixes the quantity (per officer, per duty position, per post, per contract) and a recovery rule (amortised, once-off to client, absorbed); per-contract items are ticked on the Contract Support Items tab with an equipment summary; the proposal prints once-off recovery as one "Setup and equipment" line; SQL in `sql/proposal_generator_accessory_cost_model.sql`; training deliberately untouched. v7.9.43 was: Accessories and Training Courses become dated items with a cost history: `in_use_date` / `retired_date` on the item, `quote_accessory_costs` / `quote_training_costs` effective-dated cost rows, new admin pages with a cost drawer, post checklists filtered by the quote's start date and showing the current cost; SQL in `sql/proposal_generator_item_costs.sql`. v7.9.42 was Increment 2: Issue numbers and freezes a proposal through the `issue_proposal()` transaction, versions supersede one another, Withdraw, a history panel with the frozen documents, a Proposal engagement on the deal and an offered move to the Proposal stage, Proposal column on the quote list; SQL in `sql/proposal_generator_inc2.sql`. v7.9.41 was Increment 1: the quote editor's Proposal tab renders a quote as the client proposal from an admin-edited template, previews it and prints it via a standalone window; per-post Required Accessories and Training checklists fed from the `quote_accessories` / `quote_training_courses` catalogues with new admin pages; SQL in `sql/proposal_generator_inc1.sql`, design in `docs/proposal-generator/DESIGN.md`. v7.9.40 was proxy compression that survives Netlify: the browser advertises `X-Focus-Gzip: 1`, the proxy hands the gateway's gzip bytes through under `X-Focus-Encoding: gzip`, `api()` inflates with `DecompressionStream`; Netlify itself does not compress function responses. v7.9.39 was the hotfix that switched the v7.9.38 `Content-Encoding` pass-through off. v7.9.38 was performance Tier 2, first objects: `dashboard_summary()` RPC feeds every dashboard widget and the Attention Workbench in one call, `engagements_labelled` view feeds Engagement History in one call; SQL in `sql/perf_tier2_*.sql`. v7.9.37 was Tier 1: `sweep_leads()` / `heartbeat()` RPCs with fallbacks, stream label in the engagement insert; SQL in `sql/perf_tier1_*.sql`. v7.9.36 was Tier 0: single dashboard render at login, embedded user-context read, parallel loaders for deal/lead/quote, one-call revenue-month upsert, client-only tab clicks, logos moved out of the HTML, minified deploy via `build.js`; see `docs/perf-review/`; as of 17 September 2026)

## Performance conventions (v7.9.36)

- **Sequential round trips are the cost that users feel** (~1.4 s per proxied call). Never chain `await api(...)` calls that only depend on an id you already have: put them in one `Promise.all`.
- `api(table, method, body, params, opts)` accepts `opts.prefer` (e.g. `'resolution=merge-duplicates,return=minimal'` for an upsert), `opts.silent` (background traffic, no busy bar) and `opts.headers` (returns `{ rows, headers }`). `apiGetAll` pages in parallel using `Content-Range`; `apiCount` counts server-side.
- A write already returns the row (`Prefer: return=representation`): splice it into memory and re-render, do not re-download the table. `reloadLeadAfterServerChange(row)` takes the PATCHed row.
- Tab, scope and layout clicks must be client-only (`renderLeadsPage({ reuse: true })`, `_dashApplyOrderToDom`).
- Netlify publishes `dist/` built by `node build.js` (esbuild-minified inline script). `index.html` remains the only source file; do not edit `dist/`.
- Tier 1 SQL lives in `sql/perf_tier1_indexes.sql`, `perf_tier1_triggers.sql`, `perf_tier1_audit_statement_level.sql`, `perf_tier1_sweeps_heartbeat.sql` (run in that order, Dev first). The client detects a missing `rpc/sweep_leads` / `rpc/heartbeat` (404) and falls back to the old per-row path, so code and SQL can ship independently.
- **Tier 2 shapes (v7.9.38).** `sql/perf_tier2_dashboard_summary.sql` installs `dashboard_summary(p_from)` → one jsonb bundle; `renderDashboard` keeps it in `window._dashBundle` and every widget reads `B ? B.<key> : await apiGet(...)`, so the per-widget reads remain the fallback when the function is absent (404 / PGRST202). A scope switch re-renders with `renderDashboard({ reuse: true })` — no traffic. `sql/perf_tier2_engagements_labelled.sql` installs the `engagements_labelled` view; `_engHistLoad` reads it when `_engHistViewAvailable()` (absent view → 404 / PGRST205 → old three-round path). In mock mode a view is served only when `_mockStore()` holds a table of that name. Label rules live in the SQL comments — keep them in step with `_engHistCat`, `leadEngLabel` and friends.
- **Proxy compression (v7.9.40).** Netlify does not compress function responses (checked in devtools on Dev: no `content-encoding` on `/sb/` replies), so `netlify/functions/sb.js` asks the Supabase gateway for gzip and, when the request carries `X-Focus-Gzip: 1`, returns the compressed bytes as-is (`isBase64Encoded`, `Content-Type: application/octet-stream`) flagged with the private header `X-Focus-Encoding: gzip`; `_apiBodyText()` in `index.html` inflates them with `DecompressionStream`. Browsers without it, and every non-2xx reply, get plain JSON (inflated in the function). `system_users` always stays plain because its body is scrubbed. `SB_GZIP=0` in the Netlify environment switches it all off. Never use `Content-Encoding` for this — the v7.9.38 attempt could not be verified through Netlify's edge.
- **Proposal Generator (v7.9.41, Increment 1).** `sql/proposal_generator_inc1.sql` installs `proposal_templates`, `proposal_template_sections`, `proposal_documents`, `proposal_document_sections` and the default template. `openQuote` loads templates, the quote's draft document and latest `quote_snapshots` row in its first round and the two per-post checklist tables in the second; an absent `proposal_templates` (404 / PGRST205) sets `_pgMissing` and hides the tab. The renderer (`pgRenderDocument`) never prices: it reads `rate_data.pricing.groups[].posts[]{name,officers,monthly}` from the snapshot and, until the calc engine writes one, falls back to `pbPostSummaryCost` under an INDICATIVE banner. Boilerplate uses `{{token}}` merge fields resolved in `pgFields`; unknown tokens render red and are listed above the preview. A draft `proposal_documents` row is created lazily on the first per-proposal edit; overrides upsert into `proposal_document_sections` with `on_conflict=document_id,section_key`. Issue / numbering / engagement side effects are Increment 2.
- **Proposal Generator (v7.9.42, Increment 2).** `sql/proposal_generator_inc2.sql` installs `person_has_permission()`, `issue_proposal(p_document_id, p_actor_id, p_content, p_html)` and `withdraw_proposal()` (all `SECURITY DEFINER`; the actor arrives as a parameter because the proxy uses the service key) and grants `issue_proposal` to Admin and Sales Manager (the script said Manager, a role that exists on neither database, until 2026-09-17). Issue is one transaction: number `P-<year>-<nnnn>` from `proposal_ref_seq`, frozen `content_json` + `rendered_html`, latest snapshot pinned, earlier issued rows → superseded, quote → `sent`. The client refuses to issue (`pgIssueBlockers`) without a snapshot, on a sandbox quote, with unknown merge fields, or while a `prompt` section still shows its template text; an absent RPC (404 / PGRST202) sets `_pgRpcMissing`. After the RPC the client re-renders with the real number and PATCHes the final HTML, then writes the ledger: a client-facing engagement (activity type matching /proposal/, `next_action_done`, contact linked via `engagement_people`) and a `confirmDialog` offer to move the deal to the stage named Proposal (never backwards). A draft after an issue takes version max+1 and is seeded from the issued version's `content_json.overrides`. Mock mode emulates both RPCs in `pgMockIssue` / `pgMockWithdraw`.
- **Dated catalogue items (v7.9.43).** `sql/proposal_generator_item_costs.sql` adds `in_use_date` / `retired_date` (inclusive; NULL = open) to `quote_accessories` and `quote_training_courses` and the cost tables `quote_accessory_costs` (`unit_cost`, `cost_basis`) and `quote_training_costs` (`cost_per_officer`, `validity_months`), one row per price change, never edited. `PB_ITEM_KINDS` describes both; `pbItemInWindow(item, asAt)` and `pbItemCurrentCost(costs, fk, id, asAt)` are judged at `pbQuoteAsAt()` (contract start date) like the rate cards. `renderPbItemCatalogPage(kind)` replaces the flat grid for these two pages; `code` is generated from the name (`pbSlug`) and never typed. The calc engine, when it lands, should read the same two helpers so the snapshot prices accessories and training off the dated rows.
- **Catalogue admin pages (v7.9.74).** `renderPbItemCatalogPage(kind, tabs)` = fetch + `pbiPaint()`; `pbiReload()` re-fetches with the same tabs; every click that changes nothing on the server calls `pbiPaint()`. State is `pbItemAdmin`, one per kind (`_pbItemAdminByKind` stashes the outgoing kind's object when the page switches kinds): `open` item, `addingCost`, `dirty` + `draft` and `costDirty` + `costDraft` (unsaved input, snapshotted on every keystroke by `pbiTouch` / `pbiTouchCost` and written back by `pbiRestore` after each repaint — never read from the DOM at paint time, another kind's inputs may still be there), `collapsed` keys `g:<groupName>` / `s:<subId>`, `tree` (as last painted, indexed by the drop handlers), `tabs`. Guards: `pbiBusy()` (either draft dirty) before anything that closes or moves the open editor or changes its subcategory options; `pbiDropMode` null while either draft is dirty. Every `PB_ITEM_KINDS` entry carries `sub`, its family in `quote_support_subcategories` (workforce / contract / service / uniform / training; `category` stays accessory-only and still filters `quote_accessories`). Uniform types and training courses carry `subcategory_id` after `sql/support_subcategories_all_kinds.sql`; Save sends it only when changed so an un-migrated table still saves. Sidebar pages: `pb_support_items` (tab strip over the three accessory kinds), `pb_standard_names` (`pbCatalogTabs` strip drawn inside `renderPbEditor`), `pb_uniform_types`, `pb_training_courses`. Quote side reads the same families: `pbChecklistGrouped(kind, …)`, the Contract tab's two lists, `pbUniformOptions` for the post row's dropdown.
- **Workforce Support Items tab (v7.9.53).** `quote_workforce_adjustments` (one row per quote and item: `mode` add | subtract | override, `quantity`, `reason`) loads into `pbWorkforceAdj`. `pbWorkforceRollup(includeAll)` builds a row per workforce item that is ticked on a post or adjusted (per-contract-basis items excluded): `auto` = Σ `pbAccQtyForPost` over ticking posts, `final` = `pbWfFinal(auto, adj)`, `level`/`label` (v7.9.70 words) ok 'from posts' | info 'added here' / 'here only' | warn 'reduced here' | bad 'no price' / 'retired' / 'not in use' | none 'not ticked' (grey; only via "show every item", or an adjustment that quotes nothing) with `warnings`. `renderWorkforceTab` draws collapsible rows with `PB_WF_LEVEL` colours and the inline adjust editor inside the open row; `pbWfAdjustSave` requires a reason and a `confirmDialog` (danger when fewer than the posts need), upserts on `quote_id,accessory_id`. `pbAccSummary` adds the delta as a 'Workforce adjustment' line so post prices stay tick-based; `pgPricing` groups it as 'Workforce support items'.
- **Training in conditions (v7.9.57).** `quote_support_item_rule_members.kind` gains `training` (`code` = `quote_training_courses.id` as text) for triggers and targets; `quote_post_training` gains `state` / `override_reason` like the accessory tick tables. `pbSupportEval()` returns a third bucket `training[postId][courseId] = rule name`, adds `training:<id>` to each post's active set (manual ticks plus auto ticks less `p.trainingOff`), and a training target always ticks per post. **Every reader of a post's training goes through `pbPostTrnSet(p)`** (checklist, proposal scope) — never `p.training`. `PB_CHECKLIST_TABLES` now carries `off`, `evalKey` and `pending` per kind so `pbChecklistCols`, `pbToggleChecklist` and the shared `pbChecklistOverrideConfirm(postId, kind, itemId)` / `pbChecklistOverrideCancel` handle both kinds; the admin editor adds a Training Courses group to both pickers via `_supTrainingGroup`. The 'on' POSTs still never send `state`, so the client runs before the SQL; a save with a training member on an old database fails on the kind check and the message names the SQL file.
- **Conditions & Rules (v7.9.66).** Supersedes the Conditional Criteria (v7.9.55) and Training in conditions (v7.9.57) bullets below and the table names in the Allowance conditions (v7.9.52) bullet: rules live in `quote_condition_rules` / `quote_condition_rule_members` and the four old tables are gone. Readers unchanged — `pbPostAccSet(p)` / `pbPostTrnSet(p)` / `pbContractAccMap()` / `pbActiveAllowSet(p)`; `pbSupportEval()` keeps its `posts` / `contract` / `training` buckets and adds `statutory` and `active`. `pbRuleParts(rule)` defaults to the new members table; `pbActiveRules(type)` reads the new rules table. Every mutation of a post's ticks or amounts must clear `_pbSupportEval` (or call `renderQuoteEditor`, which does). Admin picker groups come from `_condItems(withPost)`, `_condItemGroups(titles)`, `_condTrainingGroup(title)`; `COND_TYPES` holds badge + hint per type.
- **Conditional Criteria (v7.9.55).** Sidebar page `pb_conditional_criteria` → `renderPbConditionalCriteriaPage()` with `_ccActive` (null | allowances | support); the router resets it so the sidebar always lands on the two tiles; `pbBackToCcGrid()`. `renderAllowRules` (unchanged rules) lives here now — the `allowance-rules` HR tile is gone. **Support Items conditions:** `quote_support_item_rules` + `quote_support_item_rule_members` (`role` trigger | target, `kind` adds `support` with `code` = `quote_accessories.id` as text); admin screen `renderSupportRules` / `_supPaint` / `_supSave` mirrors the allowance editor with one rule type (auto-includes). `pbSupportEval()` is one fixpoint over the quote, memoised in `_pbSupportEval` (cleared at the top of the `renderQuoteEditor` datalist wrapper — the wrapper, not the original function, is the render that runs): `posts[postId][accId] = rule name` for workforce targets, `contract[accId]` for contract / service (or legacy per-contract-basis) targets, triggers judged with `pbTriggerActive` over `pbActiveAllowSet(p)` plus `support:<id>` for effective ticks (a contract-level tick holds on every post). **Every reader of ticks goes through `pbPostAccSet(p)` / `pbContractAccMap()`** (summary, workforce rollup, checklists, Contract tab, proposal scope) — never `p.accessories` / `pbContractAcc` directly, which hold only the manual rows. Overrides: `quote_post_accessories` / `quote_contract_accessories` gain `state` ('on' default, 'off') and `override_reason`; loader fills `p.accessoriesOff` `{id: reason}` and `pbContractAccOff` Map; unticking an auto item opens `pbSupportReasonRow` (confirm → DELETE then POST the off row), re-ticking deletes the off row. The 'on' POSTs never send `state`, so the client runs before the SQL (no conditions, no overrides). `pbRuleParts(rule, table)` takes the members table; `pbAllowLabelFor('support', id)` names the item.
- **Allowance conditions (v7.9.52).** `quote_allowance_rules.rule_type` ∈ exclusive | requires | implies; members carry `role` (member | trigger | target) and kind may be `post` (codes in `PB_POST_CONDITIONS`, judged by `pbPostCondition`; v7.9.54 adds `staffed`, always true, so an implies rule on it ticks its targets on every post and a requires rule on it never lets its targets be cleared). `pbImpliedStat(p)` = statutory codes ticked by an implies rule with an active trigger (iterated, so chains work); `pbStatIsAuto(r, night, implied)` replaces the bare night check in the render and in `pbActiveAllowSet(p, implied)`. `pbRuleConflict` returns `{rule, other, message}` for exclusive clashes and missing requires targets; `pbRuleRemovalBlock` refuses clearing a requires target while its trigger is active (an implies target is overridden off with a reason like night). Admin editor `_arPaint` has a type select; implies targets are statutory only.
- **Support item categories (v7.9.51).** `PB_ITEM_KINDS` has `accessory` (workforce), `contract` and `service` over the same `quote_accessories` table, filtered by `category` (`pbItemCategory(item)`, NULL = workforce); `model:'accessory'` picks the cost form, `defaultBasis` preselects the basis. Per-post checklists show workforce items only; `renderContractItemsTab` lists contract and service items (plus any legacy per-contract-basis item). Admin pages `pb_accessories`, `pb_contract_items`, `pb_service_items`.
- **Accessory cost model (v7.9.44).** `quote_accessory_costs` rows: `acquisition_cost`, `life_months`, `monthly_cost`, `loss_pct_per_year`, `cost_basis` (per_officer | per_duty_position | per_post | per_contract), `recovery` (amortised | once_off | absorbed). `pbAccMonthlyUnit(c)` = acquisition/life + running + acquisition×loss%/12; `pbAccQtyForPost(basis, p)` = officers required (pool excluded, Richard 2026-09-11) / peak headcount / 1. Per-contract items live in `quote_contract_accessories` (`pbContractAcc` Map, Contract Support Items tab). `pbAccSummary()` rolls every tick into monthly-by-post, contract monthly and once-off setup; the indicative pricing path adds those to the post prices and prints once-off as one "Setup and equipment" line (a snapshot supplies `pricing.setup_once_off`). Training costs are a separate, later model — do not fold them into this one.
- **Revenue Stream Generator (v7.9.45).** `rsgRefresh()` (on service change, start-date change and panel open) reads `rsgRevenueType(sub)` (`service_sub.revenue_type`, falling back to `is_recurring`), fills a blank escalation from `rsgDefaultEscalation(startDate)` (row for the start year, else the nearest earlier year) and shows `rsgMarginRule(sub)` (service-type rule beats the revenue-type rule; newest effective date wins). `rsgAutoPopulate()` asks for the margin with `promptDialog`, refuses one outside the rule, runs the existing `autoPopulateGrid()`, then caps `opp-probability` at `rsgProbabilityCap()`. `rsgClear()` deletes the stream's `revenue_stream_months` on the server and empties `oppMonths` (a save only upserts months with values, so it cannot clear). Input ids `opp-base-amount`, `opp-order-date`, `opp-start-date`, `opp-num-months`, `opp-escalation`, `opp-margin-pct` are unchanged (v7.9.47: they live inside the static `#rsg-modal` overlay, shown/hidden by `rsgOpen()` / `rsgClose()`, never rebuilt; `#rsg-service-sub` mirrors `opp-service-sub-id` both ways via `rsgServiceChanged`; `#rsg-summary` is the one-line state beside the button; the grid's `.rev-margin-pct` spans are refreshed in `recalcGrid`); `opp-order-date-general` mirrors the order date via `rsgSyncOrderDate`. Admin: HR tile `annual-escalation`, page `pb_margin_rules` (generic editor; `PB_SCHEMAS` columns may carry `default`, and lookups add as null). Project generator is a placeholder.
- **Suspension (v7.9.50).** `dealStartDateStale(deal)` = open stage (category Opportunity-Open, not Suspended) and `start_date <= today`. `suspendDeal` PATCHes stage → Suspended, probability 0, `suspended_from_stage_id` / `suspended_probability` / `suspended_at`, and logs a closed engagement. `sweepStaleStartDeals()` runs once per session from `renderDashboard` for the current user's own deals, then `reviewSuspendedDeals` shows `reviewSuspendedDeal` (modal: new order/start dates, shift-stream tick, Keep suspended / Restore with `confirmDialog`). `restoreSuspendedDeal` reinstates stage and probability, clears the `suspended_*` columns, optionally `shiftRevenueStreamTo(dealId, 'YYYY-MM')` (delete + reinsert the stream's months, values kept), logs a Restored engagement. `oppSuspensionOnOpen()` runs at the end of `openOpportunity`: suspends a stale deal the user may edit and shows `#opp-suspended-banner` with the review button. `saveOpportunity` enforces `checkDatesOrder` and offers the restore when a suspended deal is saved with a future start date. Absent Suspended stage (SQL not run) → all of it is inert.
- **Escalation by service type (v7.9.46).** `rsgEscalationRowsFor(sub)` returns the most specific scope with rows (sub, then major, then the general rows with both NULL); `rsgDefaultEscalation(startDate, sub)` reads that. `srRenderEscalationBand('sr-escalation')` draws the Manpower band on `renderSalaryRateGrid` (major found by name /manpower/), inline PATCH per year and `+ Year` POST; the HR tile edits every scope. Unique index is on (year, COALESCE(major,0), COALESCE(sub,0)).
- **Proving a new server-side shape:** `tests/perf/` has the harness — dump a Dev snapshot, render the old path in mock mode over it and the new path over the real SQL output, and diff the HTML (`equiv_dash.js`, `equiv_eh.js`). Ties in undefined sort order and deliberate truncation are the only acceptable differences.
- The ranked proposals for the remaining tiers (views/RPCs, topology, rendering) are in `docs/perf-review/PERFORMANCE_REVIEW.md`.
- **Tagline:** Lead by Example
- **Font:** Arial
- **Brand colours:**
  - Red: `#FA0A11`
  - Dark blue: `#003057`
  - White: `#FFFFFF`
  - Black: `#000000`
  - Khaki: `#C3B091`

## Deployment

- **Frontend:** Netlify — `storied-griffin-6eab6b.netlify.app`
- **Backend:** Supabase — `kevrfdjqyuhmgziqxuvs.supabase.co`
- **Repo:** GitHub — `Richard-Groenewald/Focus` (previously `Richard-Groenewald/salesflow`)

## Architecture

- **Single HTML file app** (legacy structure — a modular rebuild is planned).
- Earlier SalesFlow build used localStorage; current build uses Supabase.

### Pipeline stages
Lead → Prospect → Secured → Lost

### Regions
Western Cape, Gauteng

### Service types and margins
- **Manpower — On-Site Guarding:** 25% margin, recurring
- **Manpower — Off-Site Control Rooms:** 30% margin, recurring
- **Technology Works — Project:** 25% margin, non-recurring

### Revenue streams are the fundamental data structure
A deal has two phases:
1. **Opportunity** (pre-securing)
2. **Contract** (post-securing) — the opportunity stream is frozen and a contract stream is auto-created.

**Base Amount and Num Months are auto-populate only.** All TCV and financial figures derive *exclusively* from revenue streams. Do not compute financials from any other field.

## Database schema (Supabase, Postgres)

### Critical naming
- The live deals table is **`deals`** — **NOT `opportunities`**. (An older handover doc had this wrong; ignore it.)
- The existing interaction log for deals is **`engagements`**.

### Core tables
- `organisations`
- `people`
- `system_users`
- `user_credentials`
- `deals`
- `opportunity_streams`
- `contract_streams`
- `engagements`
- `interactions`
- `risks`

### Leads Register tables (added Increment 1)
- `lead_sources`
- `research_campaigns`
- `leads`
- `lead_interactions`

### Conventions
- **IDs:** `BIGINT` / `BIGSERIAL` throughout.
- **Timestamps:** `TIMESTAMPTZ` for all audit fields.
- **Ownership / audit:** `owner_id` and `created_by` always reference `people(id)`.
- **Current user resolution:** `currentUser.personId` is sourced from `system_users.person_id → people.id`.

## Critical gotchas

### Supabase keys from Netlify
- `sb_secret_` prefixed keys **fail** when called from Netlify functions.
- **Use the service role JWT** as `SUPABASE_SECRET_KEY` instead.

### Table naming
- Always write `deals`, never `opportunities`, when referencing the deals table.

## Deploy workflow

- Pushes go via a `push_to_github.js` script in the repo.
- When generating a new `push_to_github.js`, always include the exact run command in a copyable code block:

```
node push_to_github.js
```

(Claude Code can also commit and push directly via the GitHub MCP once configured — see `.mcp.json`.)

## Active work — Leads Register

**Increment 1 — shipped:**
- DB tables (`lead_sources`, `research_campaigns`, `leads`, `lead_interactions`)
- Sidebar entry
- Read-only list pages

**Increment 2 — in progress:**
- Lead form with three tabs:
  1. **Overview**
  2. **Qualification** — F / T / A / C dots
  3. **Interactions**
- Research campaign form
- Conditional dropdown shown when `source = Research`

**Beyond Increment 2:** Full rebuild from scratch planned. Modular file structure from day one.

## Coding conventions

- Match existing code style in the single HTML file.
- Brand colours and Arial font in all UI.
- All financial calculations come from revenue streams, never from Base Amount or Num Months directly.
- New tables follow the BIGINT / TIMESTAMPTZ / `people(id)` references convention above.

## What NOT to do

- Do not reference an `opportunities` table — it does not exist in this database.
- Do not use `sb_secret_` keys in any Netlify-deployed code path.
- Do not compute TCV or revenue figures from Base Amount or Num Months.
- Do not commit secrets. The `.env` and any keys stay local; Netlify env vars hold production secrets.
